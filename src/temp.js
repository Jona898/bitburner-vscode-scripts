/** @param {NS} ns */
export async function main(ns) {
  ns.tail()











  // for (let i = 1; i <= 1024; i *= 2) {

  //   // ns.tprint(`${i} Threads ${ns.formatNumber(ns.growthAnalyze(i, "omega-net"), 6)}`)
  //   // ns.tprint(`${i} Threads ${ns.formatNumber(ns.weakenAnalyze(i, 1), 6)}`)
  // }

  // let hostnames = ["nectar-net"]

  // hostnames.push(...ns.scan(hostnames[0]))

  // for (let host of hostnames) {
  //   // ns.tprint(`${host.padStart(15)}: w${ns.formatNumber(ns.weakenAnalyze(1, 1), 6)} g${ns.formatNumber(ns.growthAnalyzeSecurity(1, host), 6)} h${ns.formatNumber(ns.hackAnalyzeSecurity(1, host), 6)}`)
  //   // ns.tprint(`${host.padStart(15)}: ${ns.hackAnalyze(host)} ${ns.hackAnalyzeChance(host)}`)

  // }






  

  gainRootRights("nectar-net")
}